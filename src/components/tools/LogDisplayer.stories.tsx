import { Meta, StoryObj } from "@storybook/react"
import LogDisplayer from "./LogDisplayer"

export default {
    title: "Tools/LogDisplayer",
    component: LogDisplayer,
} as Meta<typeof LogDisplayer>

export const Default: StoryObj<typeof LogDisplayer> = {}